# P14 Validation

## Automated gates

| Check | Command | Result |
|---|---|---|
| TypeScript | `yarn typecheck` | **PASS** — no diagnostics. |
| Lint | `yarn lint` | **PASS** — ESLint completed cleanly. |
| Tests | `yarn test` | **PASS** — 14 suites, 114 tests passed. |
| Expo diagnostics | `npx expo-doctor` | **PASS** — 21/21 checks passed. |

All required P14 gates passed after adding the blocked microphone permission configuration.

## Harmless configuration/environment checks

| Check | Result |
|---|---|
| `npx expo config --type public` | **PASS** — final identity, SDK 57 config, plugins, EAS project ID, and `android.blockedPermissions` resolved; package-level permissions still appear in resolved input before manifest filtering. |
| `npx -y eas-cli@24.3.0 --version` | **PASS** — `eas-cli/24.3.0`; no build started. |
| `git check-ignore` for preview APK/build outputs | **PASS** — `build-outputs/`, `*.apk`, and `*.aab` ignored. |
| Node inspection | `v24.15.0`; differs from historical Node 22 baseline. |
| Java inspection | `21.0.8`; differs from historical Java 17 baseline. |
| ADB inspection | `1.0.41` installed. |
| Disk inspection | `$HOME`: 34G free; `/tmp`: 12G free at audit time. |

## Build/install/smoke status

- Preview APK build: **NOT EXECUTED BY OMP**.
- Preview APK installation: **NOT EXECUTED BY OMP**.
- Physical Android smoke: **PENDING USER**.
- Expected artifact: `build-outputs/preview/avistaves-preview.apk`.

## Prohibited commands not run

- `yarn build:dev`
- `yarn build:preview`
- `eas build ...`
- `npx eas-cli build ...`
- `npx expo run:android`
- `expo prebuild`
- Gradle assemble/bundle commands
- `expo start --web`
- `expo export --platform web`

No APK/AAB was generated by OMP.
