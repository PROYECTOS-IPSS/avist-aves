# P2.1 — Local Android Development/Preview Build Infrastructure

- **Previous phase:** P2
- **Current phase:** P2.1
- **Next phase:** P3

## Objective

Configure Android development and preview build infrastructure without compiling either build.

## Scope

Implemented:

- `expo-dev-client` for the project development runtime.
- EAS local development and preview profiles.
- Android package identifier `com.wuanpack.avistaves`.
- `yarn build:dev` and `yarn build:preview` scripts.
- Deterministic `build-outputs/development/` and `build-outputs/preview/` output files.
- `yarn start` directed to `expo start --dev-client`.
- Git exclusions for local outputs, APK, and AAB artifacts.
- Manual workflow documentation.

No build command was executed. No APK or AAB was generated.

## Final configuration

- Expo SDK: `57.0.0`
- expo-dev-client: `~57.0.19`
- EAS CLI checked: `24.3.0`
- Managed workflow preserved: yes
- Development profile: development client, internal distribution, APK
- Preview profile: internal distribution, APK
- Development output: `build-outputs/development/avistaves-development.apk`
- Preview output: `build-outputs/preview/avistaves-preview.apk`

## Quality gates

- `yarn typecheck`: PASS
- `yarn lint`: PASS
- `yarn test`: PASS — 3 suites, 9 tests
- `npx expo-doctor`: PASS — 21/21 checks
- Static EAS/profile checks: PASS
- Expo Web validation: intentionally skipped

## Important execution policy

Configuration only:

- build:dev configured: **YES**
- build:preview configured: **YES**
- build:dev executed by OMP: **NO**
- build:preview executed by OMP: **NO**
- APK generated by OMP: **NO**
- AAB generated by OMP: **NO**

Absence of APK/AAB is expected and is not a P2.1 failure.

## Risks

- EAS local builds require a correctly provisioned local Android/JDK environment when the user runs them.
- EAS CLI reports the project is not linked to an EAS project during non-interactive config display. No remote project, credentials, or authentication were created in P2.1.
- `build:dev` and `build:preview` remain unexecuted until manual user validation.

**READY FOR P3: YES**
